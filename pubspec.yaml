name: smartcalc_pro
description: "SmartCalc Pro - Advanced Calculator with Scientific Functions, Dark/Light Mode & Unit Converter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2

  # ✅ Matematiksel ifadeleri değerlendirmek için
  math_expressions: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
